video文件夹下包括所有训练和测试时用到的视频，每个文件夹代表一个视频，每个文件夹下存放的是该视频抽完帧的图片
train.txt文件是用于训练的标签，每行为一个（视频）标签，格式为：视频所在文件夹 该视频所属标签
test.txt文件是用于测试的视频所在文件夹名称，每行格式为：视频所在文件夹
submission.txt文件是用于提交的示例文件，每行格式为：视频所在文件夹 该视频的分类结果
labels.txt文件是所有视频标签集合，每行代表一个视频标签（类别），一共174类
